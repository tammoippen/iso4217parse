name: Publish

on:
  push:
    tags:
      - v*
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Set Up uv and Python 3.12
        uses: astral-sh/setup-uv@v10.0.1
        with:
          python-version: 3.12

      - name: Build
        run: uv build -v

      - name: Archive artifacts
        uses: actions/upload-artifact@v6
        with:
          path: dist/*

      - name: Publish
        run: uv publish -v -t ${{ secrets.PYPI_PASS }}
